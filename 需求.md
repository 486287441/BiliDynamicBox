# B站动态收件箱插件 — 产品需求（PRD）

> 由 `粗糙需求.md` 整理，并经 `对话 html/index.html` 产品选题更新。  
> `[待确认]` → 用户 HTML 提交后改为 `[已定]`。  
> **技术栈与实现路线不在本文** → 见 `plan/技术方案.md`（阶段 2 撰写）。

---

## 1. 背景与目标

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 要解决的问题 | [已定] | B站动态页信息噪音高、决策成本高，用户希望在同一页面快速筛选“想看 / 不想看”。 |
| 成功时用户得到什么 | [已定] | 通过卡片化动态收件箱快速筛选视频动态，并形成可持续的“想看清单 + 不想看过滤”体验。 |

---

## 2. 用户与典型场景

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 目标用户 | [待确认] | 高频浏览 B站动态、希望快速做观看决策的用户。 |
| 典型场景（1–3 个） | [已定] | 1) 打开动态页集中处理今日更新；2) 划分“稍后再看”与“不想看”；3) 连续过滤低兴趣 UP 内容。 |

---

## 3. 功能范围（用户能做什么）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 核心流程 | [已定] | 在 `https://t.bilibili.com/` 看到按日期分组的卡片流，只处理视频动态，对每张卡片执行“想看 / 不想看”。 |
| 关键页面/状态 | [已定] | 仅替换动态页内容区；卡片展示封面、标题、UP 主、发布时间；单页纵向滚动展示分组；图片动态与纯文字动态不展示。 |
| 边界与例外 | [已定] | 仅在动态页生效；“不想看”卡片刷新后不再出现；同一 UP 连续 5 次“不想看”后触发取关确认。 |

---

## 4. 体验与交互原则

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 交互方式（键盘/触控等） | [已定] | 以点击按钮为主，当前不要求额外键盘快捷操作。 |
| 视觉/氛围期望 | [已定] | 参考 BewlyBewly 卡片风格：简洁网格、信息层级清晰、便于快速扫读。 |
| 进度与反馈 | [已定] | 点击“想看”后使用轻提示（toast）反馈；垃圾箱入口固定在页面顶部工具栏。 |

---

## 5. 明确不做（范围外）

| 项 | 说明 |
| --- | --- |
| 改造非动态页 | 仅处理 `https://t.bilibili.com/`，不扩展到首页/搜索页。 |
| 复杂社交运营能力 | 当前范围不含推荐算法、批量管理、社区协作等扩展能力。 |

---

## 6. 产品决策记录（HTML 提交后填写）

| 题号 | 问题（产品向） | 选择 | 用户可理解含义 |
| --- | --- | --- | --- |
| q_primary_goal | 这个插件最优先帮你完成什么？ | `quick_filter` | 优先快速筛掉不感兴趣动态，降低浏览噪音。 |
| q_grouping_mode | 日期分组希望细到什么程度？ | `today_yesterday_then_date` | 分组固定为“今天 / 昨天 / 更早按具体日期”。 |
| q_card_density | 卡片一屏显示密度偏好？ | `balanced_default` | 使用中等密度，兼顾信息量与可读性。 |
| q_dislike_scope | “不想看”后希望隐藏到什么范围？ | `hide_only_this_post` | 仅隐藏当前这条动态，不连带屏蔽该 UP 其他内容。 |
| q_trashbox_entry | 垃圾箱入口放在哪最顺手？ | `top_toolbar` | 垃圾箱入口放在页面顶部工具栏，随时可见。 |
| q_unfollow_trigger_behavior | 连续 5 次“不想看”后的动作策略？ | `immediate_modal` | 第 5 次当下立刻弹窗确认是否取关。 |
| q_watch_action_feedback | 点击“想看”后你最想看到什么反馈？ | `toast_only` | 操作后只显示轻量 toast 提示，不打断浏览。 |

---

## 7. 成功标准（需求级验收）

完成本 PRD 即表示：

1. 用户可在动态页内按日期分组浏览并进行“想看 / 不想看”决策。  
2. “不想看”过滤与 UP 连续计数行为有明确且一致的产品规则。  
3. 关键交互反馈、信息密度、垃圾箱入口等体验决策已拍板。  
4. 内容范围明确为“仅视频动态”，图片和纯文字动态不进入收件箱卡片流。

（阶段 2 的 `plan/Mxx-<模块名>.md` 拆实现步骤；`plan/技术方案.md` 定技术栈。）

---

## 8. 技术实现（待 plan 阶段）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 技术栈 | 见 plan | 由 Agent 在 `plan/技术方案.md` 根据 PRD 撰写 |
| 部署与运行 | 见 plan | |
| 数据与存储 | 见 plan | |
