# DeepSeek API keys are entered in the extension's "设置 AI" panel and stored
# in chrome.storage.local. Never put a real key in source code or build-time env:
# browser extension bundles can be inspected by anyone who receives the package.
