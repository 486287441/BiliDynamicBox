# This extension uses the logged-in Bilibili session in the browser.
# No API keys or tokens are required in environment variables.
# Copy to .env only if you add local-only tooling later (never commit .env).
