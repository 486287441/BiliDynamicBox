---
description: 改动 extension 源码后必须自行 npm run build 验证
alwaysApply: true
---

# Extension 构建验证

当用户要求改动代码（实现功能、修 bug、重构等，不含纯问答/讨论）时：

1. 若改动涉及 `extension/` 源码或会影响构建产物，**必须在完成改动后自行执行构建**，不要等用户提醒。
2. 在仓库根目录或 `extension/` 下执行：

   ```bash
   cd extension
   npm run build
   ```

3. 若构建失败，修复问题并 **重新 build**，直到通过或明确向用户说明无法修复的阻塞原因。
4. 在回复中简要说明构建是否成功；若失败，附上关键错误信息。

## 例外

- 用户明确说「先别 build」「只讨论不写代码」时，不执行构建。
- 仅修改与扩展无关的文档（如根目录 `plan/`、`需求.md`）且未动 `extension/src` 时，可不 build。
