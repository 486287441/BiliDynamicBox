# B站动态收件箱插件 · 需求文档

## 作用范围

仅在 [https://t.bilibili.com/](https://t.bilibili.com/) 页面生效，替换原版动态页的内容区域。

## 视频卡片样式

参考 BewlyBewly 插件的卡片风格：

封面图、视频标题、UP主名称、发布时间，布局简洁，以卡片网格形式展示。D:\code\billbillRead\Bewly卡片样式图片.png

## 核心功能：按日期分组

动态内容按发布日期分组展示，结构如下：

  今天

  ───────────────────

  [卡片] [卡片] [卡片]

  昨天

  ───────────────────

  [卡片] [卡片]

  5月26日

  ───────────────────

  ...

从上到下单页滚动，无需 Tab 切换。

## 核心功能：卡片决策操作

每张卡片提供两个操作：

- 「想看」：将该视频加入 B站稍后再看列表
- 「不想看」：将该卡片从页面移除

这两个按钮常驻在卡片底部
  在 UP主名 / 时间 那一行的下面，固定放两个小按钮，一直可见。

## 核心功能：自动清理 UP 主

同一个 UP 主的视频连续被标记「不想看」达到 5 次时，

弹出确认框："是否取关 [UP主名]？"

用户确认后，执行取关操作。

## 数据持久化

- 已标记「不想看」的视频不再重复出现，而是放进垃圾箱里面（刷新页面后也不显示）
- 每个 UP 主的「不想看」计数需持久保存

在某一个地方添加一个小的垃圾箱入口